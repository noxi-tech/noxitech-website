<nav class="fixed top-6 left-0 right-0 z-50 mx-auto max-w-7xl px-6">
  <div
    class="relative z-50 glass rounded-full px-6 py-2.5 flex justify-between items-center shadow-2xl shadow-purple-500/5"
  >
    <a
      href="/"
      class="text-xl font-bold text-white tracking-tight flex items-center gap-2"
    >
      <img src="/assets/light_logo.svg" alt="Noxitech Logo" class="w-8 h-8" />
      <span>Noxitech</span>
    </a>

    <div class="hidden lg:flex items-center space-x-1">
      <a
        href="/"
        class="text-sm font-medium px-4 py-2 rounded-full text-white hover:bg-white/10 transition-all"
        >Home</a
      >
      <a
        href="/about"
        class="text-sm font-medium px-4 py-2 rounded-full text-white hover:bg-white/10 transition-all"
        >About</a
      >
      <a
        href="/services"
        class="text-sm font-medium px-4 py-2 rounded-full text-white hover:bg-white/10 transition-all"
        >How It Works</a
      >
      <a
        href="/blog"
        class="text-sm font-medium px-4 py-2 rounded-full text-white hover:bg-white/10 transition-all"
        >Blog</a
      >
      <a
        href="/faqs"
        class="text-sm font-medium px-4 py-2 rounded-full text-white hover:bg-white/10 transition-all"
        >FAQs</a
      >
      <a
        href="/contact"
        class="text-sm font-medium px-4 py-2 rounded-full text-white hover:bg-white/10 transition-all"
        >Contact</a
      >
    </div>

    <div class="flex items-center space-x-2 lg:space-x-4">
      <a
        href="/contact"
        class="hidden! lg:inline-flex! btn-primary px-6! py-2.5! text-sm!"
      >
        Book a Free Audit
      </a>
      <button
        id="mobile-menu-btn"
        aria-label="Open menu"
        class="lg:hidden text-white p-2 focus:outline-none shrink-0 cursor-pointer"
      >
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="h-6 w-6 block transition-transform duration-300 hover:scale-110"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M4 6h16M4 12h16m-7 6h7"></path>
        </svg>
      </button>
    </div>
  </div>
</nav>

<!-- Full-screen Mobile menu -->
<div
  id="mobile-menu"
  class="lg:hidden fixed inset-0 z-999 bg-slate-900/60 backdrop-blur-3xl hidden flex-col justify-center items-center opacity-0 transition-opacity duration-300"
>
  <!-- Close button inside the overlay -->
  <button
    id="mobile-menu-close-btn"
    aria-label="Close menu"
    class="absolute top-8 right-8 text-white/70 hover:text-white p-2 focus:outline-none cursor-pointer transition-colors"
  >
    <svg
      xmlns="http://www.w3.org/2000/svg"
      class="h-10 w-10 transition-transform duration-300 hover:rotate-90"
      fill="none"
      viewBox="0 0 24 24"
      stroke="currentColor"
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M6 18L18 6M6 6l12 12"></path>
    </svg>
  </button>

  <div
    class="flex flex-col items-center justify-center space-y-8 text-center px-6 w-full max-w-sm"
  >
    <a
      href="/"
      class="text-4xl font-bold tracking-tight text-white hover:text-purple-400 hover:scale-105 transition-all duration-300"
      >Home</a
    >
    <a
      href="/about"
      class="text-4xl font-bold tracking-tight text-white hover:text-purple-400 hover:scale-105 transition-all duration-300"
      >About</a
    >
    <a
      href="/services"
      class="text-4xl font-bold tracking-tight text-white hover:text-purple-400 hover:scale-105 transition-all duration-300"
      >How It Works</a
    >
    <a
      href="/blog"
      class="text-4xl font-bold tracking-tight text-white hover:text-purple-400 hover:scale-105 transition-all duration-300"
      >Blog</a
    >
    <a
      href="/faqs"
      class="text-4xl font-bold tracking-tight text-white hover:text-purple-400 hover:scale-105 transition-all duration-300"
      >FAQs</a
    >
    <a
      href="/contact"
      class="text-4xl font-bold tracking-tight text-white hover:text-purple-400 hover:scale-105 transition-all duration-300"
      >Contact</a
    >
    <div class="pt-8 w-full">
      <a
        href="/contact"
        class="btn-primary w-full flex justify-center py-4! text-xl! shadow-xl shadow-purple-500/20"
      >
        Book a Free Audit
      </a>
    </div>
  </div>
</div>

<script>
  // Ensure the script runs after Astro ViewTransitions updates the page
  document.addEventListener("astro:page-load", () => {
    const openBtn = document.getElementById("mobile-menu-btn");
    const closeBtn = document.getElementById("mobile-menu-close-btn");
    const menu = document.getElementById("mobile-menu");

    if (openBtn && closeBtn && menu) {
      const openMenu = () => {
        // Swap hidden for flex so justify-center works
        menu.classList.remove("hidden");
        menu.classList.add("flex");
        void menu.offsetWidth; // Trigger reflow
        menu.classList.remove("opacity-0");
        menu.classList.add("opacity-100");
        document.body.style.overflow = "hidden";
      };

      const closeMenu = () => {
        menu.classList.remove("opacity-100");
        menu.classList.add("opacity-0");
        document.body.style.overflow = "";
        setTimeout(() => {
          menu.classList.remove("flex");
          menu.classList.add("hidden");
        }, 300);
      };

      openBtn.addEventListener("click", openMenu);
      closeBtn.addEventListener("click", closeMenu);

      const links = menu.querySelectorAll("a");
      links.forEach((link) => {
        link.addEventListener("click", closeMenu);
      });
    }
  });
</script>
